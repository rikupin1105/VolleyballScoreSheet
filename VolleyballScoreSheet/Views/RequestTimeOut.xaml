<UserControl
    x:Class="VolleyballScoreSheet.Views.RequestTimeOut"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:VolleyballScoreSheet.Converter"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <local:HiddenOrVissible x:Key="HiddenOrVissible" />
    </UserControl.Resources>
    <StackPanel>
        <TextBlock
            HorizontalAlignment="Center"
            FontSize="40"
            FontWeight="Black"
            Text="Request Timeout for" />
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <Button
                Margin="10"
                Background="{Binding LeftTeamColor.Value}"
                Content="{Binding LeftTeamName.Value}"
                FontSize="40"
                Foreground="White" />
            <Button
                Margin="10"
                Background="{Binding RightTeamColor.Value}"
                Content="{Binding RightTeamName.Value}"
                FontSize="40"
                Foreground="White" />
        </StackPanel>
        <Button
            HorizontalAlignment="right"
            VerticalAlignment="Bottom"
            Content="キャンセル" />
    </StackPanel>
</UserControl>
